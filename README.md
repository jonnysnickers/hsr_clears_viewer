If you can run localhost server then you can use index.html and it will fetch data.csv
Otherwise just open index_with_data.html in your browser.

embed_data.py can be used to put data.csv into index.html to create index_with_data.html
